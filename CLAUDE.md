## Claude Code

Follow the project rules in this repository and any more specific `CLAUDE.md` files in subdirectories.

Also apply:
- `.claude/CLAUDE.md`
- applicable files under `.claude/rules/`

Default behavior:
- Prefer minimal, affected-scope validation.
- Do not run full workspace tests unless the change is broad or explicitly requires it.
- When a subdirectory has a more specific `CLAUDE.md`, follow the more specific file.
